import QtQuick
import UIverse.Core

SlotRect {
    radius: tokens.radiusSm
    color: "transparent"
    border.width: tokens.borderWidth
    border.color: tokens.statusColor(variant, tokens.border)
}
